---
title: Anniversary Blogs
---
<div class="container">
  <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-4 g-4 mb-5">
    {% for post in collections.blogs %}
      <div class="col">
        <a class="card-link" href="{{ post.url }}">
          <div class="card">
            <img src="{{ base_path }}{{ post.data.image.src }}" class="card-img-top" alt="{{ post.data.image.alt }}">
            <div class="card-body">
              <h5 class="card-title">{{ post.data.title }}</h5>
              <p class="card-text">{{ post.data.description }}</p>
            </div>
          </div>
        </a>
      </div>
    {% endfor %}
  </div>
</div>
